# Sistema Ferias - repositorio inicial
Stack: React (Vite) + Flask + MySQL (scripts de inicialización).

Carpetas:
- backend/: API Flask
- frontend/: React (Vite)
- database/: schema.sql y seed_data.sql

Levantar con Docker: `docker-compose up --build`